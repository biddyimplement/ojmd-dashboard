
---
spring:
  profiles:
    active: postgres-cloud
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create

# turn off the security on the actuator end points as this is a demo app
management:
  security:
    enabled: false